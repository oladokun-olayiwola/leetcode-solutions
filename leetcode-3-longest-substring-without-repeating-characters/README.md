List solution for [LeetCode #3: Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/) has a time complexity of **O(n²)**.

The `for` loop has a time complexity of **O(n)**, and `subList()` has a time complexity of **O(n)**. Therefore, the overall time complexity is **O(n²)**.

The solution times out on LeetCode.

*(Working on a new solution.)*